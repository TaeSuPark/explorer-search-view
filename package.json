{
  "name": "filtered-explorer",
  "displayName": "Filtered Explorer",
  "description": "폴더 이름으로 필터링되는 파일 탐색기",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "filtered-explorer.searchFolders",
        "title": "폴더 필터링",
        "icon": "$(search)"
      },
      {
        "command": "filtered-explorer.pinFolder",
        "title": "폴더 고정하기",
        "icon": "$(pin)"
      },
      {
        "command": "filtered-explorer.unpinFolder",
        "title": "고정 해제",
        "icon": "$(pinned)"
      },
      {
        "command": "filtered-explorer.moveUp",
        "title": "위로 이동",
        "icon": "$(arrow-up)"
      },
      {
        "command": "filtered-explorer.moveDown",
        "title": "아래로 이동",
        "icon": "$(arrow-down)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "filtered-explorer",
          "title": "필터링된 탐색기",
          "icon": "$(files)"
        }
      ]
    },
    "views": {
      "filtered-explorer": [
        {
          "id": "filteredExplorerView",
          "name": "필터링된 탐색기",
          "icon": "$(files)"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "filtered-explorer.searchFolders",
          "when": "view == filteredExplorerView",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "filtered-explorer.searchFolders",
          "title": "폴더 검색"
        }
      ],
      "view/item/context": [
        {
          "command": "filtered-explorer.pinFolder",
          "when": "view == filteredExplorerView && viewItem == folder",
          "group": "inline"
        },
        {
          "command": "filtered-explorer.unpinFolder",
          "when": "view == filteredExplorerView && viewItem == folder-pinned",
          "group": "inline"
        },
        {
          "command": "filtered-explorer.moveUp",
          "when": "view == filteredExplorerView && viewItem == folder-pinned",
          "group": "inline"
        },
        {
          "command": "filtered-explorer.moveDown",
          "when": "view == filteredExplorerView && viewItem == folder-pinned",
          "group": "inline"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.97.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.22.0",
    "@typescript-eslint/parser": "^8.22.0",
    "eslint": "^9.19.0",
    "typescript": "^5.7.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  }
}
